import { createTheme } from "@mui/material";

const muiTheme = createTheme({
  palette: {
    primary: {
      main: "#ff6347",
    },
    secondary: {
      main: "#ff6347",
    },
    normal: {
      main: "#888",
    },
    black: {
      main: "#000",
    }
  },
});

export default muiTheme;